<div class="row">
    <div class="col-lg-6">


        <form method="post" action="/login">
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <div class="form-group">
                <label for="address">Email address</label>
                <input type="email" class="form-control {{#if errors.address}}is-invalid{{/if}}" name="address" id="address" aria-describedby="emailHelp" placeholder="Enter email">
                {{#if errors.address}}
                    <div class="invalid-feedback">
                        {{errors.address}}
                    </div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" class="form-control {{#if errors.password}}is-invalid{{/if}}" name="password" id="password" placeholder="Password">
                {{#if errors.password}}
                    <div class="invalid-feedback">
                        {{errors.password}}
                    </div>
                {{/if}}
            </div>
            <div class="form-check">
                <label class="form-check-label">
            <input type="checkbox" name="remember" class="form-check-input">
            Remember me
        </label>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary"><span class="oi oi-account-login" title="Ethereal" aria-hidden="true"></span> Log in</button>
            </div>
        </form>
    </div>
</div>

<p>
    &nbsp;
</p>

<form method="post" action="/create">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <h5>
        Don't have an Ethereal user account yet?
    </h5>
    <p>
        Create one from <a href="https://nodemailer.com/about/#example">Nodemailer</a> with the <code>createTestAccount</code> method or <button type="submit" class="btn btn-primary btn-sm"><span class="oi oi-plus" title="Ethereal" aria-hidden="true"></span> click here</button> to create an user
    </p>
</form>
